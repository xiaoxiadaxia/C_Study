//对字符串中的字符的存取--指针法
#include<stdio.h>
main(){
	char a[]="Program",b[20],*p1,*p2;
	p1=a;
	p2=b;
	for(;*p1!='\0';p1++,p2++)//++和*的优先级是一样的，都是自右向左的结合方式*p++=*(p++) 
		*p2=*p1;
	*p2='\0';//这一步不太懂,我现在懂了，它就是一个变量，指向地址，而地址在变 
	printf("%s",a);
} 
